# Digital Agency Rubikon - Products SPA

SPA приложение для управления продуктами с использованием React, TypeScript и Zustand.

## 🚀 Функционал

### Основные возможности:

- 📋 **Список продуктов** - отображение карточек продуктов с изображениями
- ❤️ **Избранное** - добавление/удаление продуктов в избранное
- 🗑️ **Удаление** - асинхронное удаление продуктов
- 🔍 **Поиск** - поиск по названию и описанию (без кнопки отправки)
- 🔽 **Фильтрация** - фильтры по избранным, цене и рейтингу
- 📄 **Пагинация** - разбивка списка на страницы (12 элементов на страницу)
- ➕ **Создание** - создание новых продуктов с валидацией
- ✏️ **Редактирование** - редактирование существующих продуктов
- 💾 **LocalStorage** - сохранение данных в браузере

## 🛠️ Технологии

- **React 18** - UI библиотека
- **TypeScript** - типизация
- **Zustand** - управление состоянием
- **React Router** - маршрутизация
- **Vite** - сборка проекта
- **CSS3** - стили с анимациями

## 📦 Установка

```bash
# Установка зависимостей (используется bun)
bun install

# Или с npm
npm install
```

## 🏃 Запуск

```bash
# Разработка
bun run dev
# или
npm run dev

# Сборка для продакшена
bun run build
# или
npm run build

# Превью продакшен сборки
bun run preview
# или
npm run preview
```

## 🌐 GitHub Pages

### Настройка для деплоя на GitHub Pages:

1. **Создайте репозиторий на GitHub**

   - Название репозитория может быть любым
   - Проект использует HashRouter, поэтому не требует настройки base path

2. **Включите GitHub Pages:**

   - Перейдите в Settings → Pages
   - Source: выберите "GitHub Actions"

3. **Деплой через GitHub Actions (автоматический):**

   - Файл `.github/workflows/deploy.yml` уже создан
   - Просто сделайте push в ветку `main`:
     ```bash
     git init
     git add .
     git commit -m "Initial commit"
     git branch -M main
     git remote add origin https://github.com/ваш-username/Digital-Agency-Rubikon.git
     git push -u origin main
     ```
   - Деплой произойдет автоматически через GitHub Actions

4. **Ручной деплой (альтернатива):**
   ```bash
   bun run build
   # Затем загрузите содержимое папки dist в ветку gh-pages
   ```

### 🔗 Ссылка на GitHub Pages:

После деплоя приложение будет доступно по адресу:

```
https://emranp.github.io/Digital-Agency-Rubikon-Products-SPA/
```

## 📁 Структура проекта

```
src/
├── components/       # React компоненты
│   ├── AdvancedFilters.tsx
│   ├── FilterBar.tsx
│   ├── Pagination.tsx
│   ├── ProductCard.tsx
│   ├── Rating.tsx
│   └── SearchBar.tsx
├── pages/            # Страницы приложения
│   ├── CreateProductPage.tsx
│   ├── EditProductPage.tsx
│   ├── ProductDetailsPage.tsx
│   └── ProductsPage.tsx
├── store/            # Zustand store
│   └── products.ts
├── types.ts          # TypeScript типы
├── App.tsx           # Главный компонент
├── main.tsx          # Точка входа
└── index.css         # Глобальные стили
```

## 🎨 Особенности UI

- Плавные анимации появления элементов
- Адаптивный дизайн
- Современный и чистый интерфейс
- Валидация форм
- Обработка состояний загрузки и ошибок

## 📝 API

Приложение использует [Fake Store API](https://fakestoreapi.com/) для получения начальных данных о продуктах.

## 📄 Лицензия

MIT
